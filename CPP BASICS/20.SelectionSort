void selectionSort(vector<int>& arr, int n)
{   
    // Write your code here.
    // int min = 10000000;
    for(int i=0;i<n-1;i++){
        // min= arr[i];
        for(int j=i+1;j<n;j++){
            if(arr[j] < arr[i]){
                swap(arr[i],arr[j]);
            }
        }
    }
}